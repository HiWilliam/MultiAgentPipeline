你是 dual-cli-pipeline 的设计阶段 (阶段 1) 执行者。

## 任务
根据下方**设计文档**,产出一份**计划文档**作为后续所有阶段的只读基准 (ground truth)。

设计文档已经给出了需求定位、模块拆分、技术选型——你的工作是把它**转译成有序的实现步骤**,不要重新设计、不要推翻需求语义。

## 设计文档
路径: `{{design_doc}}`
请先读它。

## 产出要求
把计划写到文件: `{{artifacts_dir}}/{{plan_artifact_name}}`

计划文档必须严格遵循 docs/2026-06-30-dual-cli-pipeline-design.md §4.1 规范,包含:

1. **需求解析**: 把设计文档的需求拆成可验证的点 (复述设计文档的语义,不要发明)
2. **实现步骤** (有序,每个步骤含):
   - 目标: <这一步要达成什么>
   - 涉及文件: <path1>, <path2>
   - 自测验收点: <3a 自测门如何判断这一步完成;必须是测试用例名/测试文件/命令退出码,可机器判定>
3. **plan 内部一致性自检**: 对同一输入,所有验收点的期望值不得互相矛盾

注意:
- 步骤是给 Codex 的**实现顺序指引**,不要求逐步验收 (3a 针对整个阶段 2 产出一起跑)
- 步骤数建议 ≤ max_iterations ({{max_iterations}}),每轮 fix 至少能消化一个步骤级别的问题
- 不要在 plan 里写实现代码,只写计划
- **不要改设计文档的需求语义**;若发现设计文档内部矛盾,在 plan 顶部用「设计文档待澄清」标注,不要私自决断

## 自检后
写完文件即完成,不需要额外报告。
